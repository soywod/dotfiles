[general]
accounts = gmail, amen, ovh
ssl = yes
starttls = yes
socktimeout = 5
pythonfile = ~/.config/mutt/credentials/imappwd.py

[Account gmail]

localrepository = gmail-local
remoterepository = gmail-remote

[Repository gmail-remote]
auth_mechanisms = LOGIN
type = Gmail
remoteuser = clement.douin@gmail.com
remotepasseval = mailpasswd("gmail")
remoteport = 993
folderfilter = lambda foldername: foldername not in ['[Gmail]/All Mail']
sslcacertfile = /etc/ssl/certs/ca-certificates.crt

[Repository gmail-local]
type = Maildir
localfolders = ~/.mail/gmail
[Account amen]

localrepository = amen-local
remoterepository = amen-remote

[Repository amen-remote]
auth_mechanisms = LOGIN
type = IMAP
ssl = no
remoteuser = clement.douin@strategypartner.eu
remotepasseval = mailpasswd("amen")
remotehost = mail-fr.securemail.pro
remoteport = 143
sslcacertfile = /etc/ssl/certs/ca-certificates.crt

[Repository amen-local]
type = Maildir
localfolders = ~/.mail/amen
[Account ovh]

localrepository = ovh-local
remoterepository = ovh-remote

[Repository ovh-remote]
auth_mechanisms = LOGIN
type = IMAP
remoteuser = contact
remotepasseval = mailpasswd("ovh")
remotehost = mail.clementdouin.ovh
remoteport = 993
sslcacertfile = /etc/ssl/certs/ca-certificates.crt

[Repository ovh-local]
type = Maildir
localfolders = ~/.mail/ovh

